import { LogoFlexRow, Template } from "@/components";

export default function PageInicio() {
    return <div>
        <Template>
            <div className="mt-10 flex w-[15rem]">
                <LogoFlexRow />
            </div>
        </Template>
    </div>
}